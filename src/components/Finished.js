import React from "react"

const Finished = (props) => {
    return (
        <div className="finished-container">
            <h1 className="welcome-message">voting has finished.</h1>
        </div>
    )
}

export default Finished